<!-- meta: {"intents":["save_version","submit_change"],"priority":20,"description":"分批提交操作指引"} -->

# 分批提交操作指引

当用户要求分多次提交时（如"先提交代码再提交文档"、"先提交文档类修改，再提交代码类修改"），遵循以下规则：

1. 每次提交只需调用一次 save_version/submit_change，完成后直接告知用户
2. 不要在两次提交之间反复查看状态确认
3. 如果用户指定了文件分类方式（按类型、按目录等），按用户的方式拆分提交
